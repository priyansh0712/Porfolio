---
phase: 01-project-foundation-base-architecture
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - package.json
  - tailwind.config.js
  - static/css/src/input.css
  - static/css/dist/styles.css
  - templates/base.html
  - templates/components/navbar.html
  - templates/components/footer.html
  - templates/components/alerts.html
autonomous: true
requirements:
  - Core project foundation
must_haves:
  truths:
    - "Tailwind CSS CLI compiles input.css into dist/styles.css using npm scripts"
    - "Base HTML layout renders Minimal White & Gray theme with subtle borders"
    - "Navigation header, alert messages container, and footer render responsively"
  artifacts:
    - path: "package.json"
      provides: "npm scripts for tailwind build and watch execution"
    - path: "tailwind.config.js"
      provides: "Tailwind content paths pointing to templates/ and apps/ HTML/JS"
    - path: "static/css/src/input.css"
      provides: "Tailwind CSS entry point (@tailwind base, components, utilities)"
    - path: "templates/base.html"
      provides: "Root HTML layout template with block areas and linked styles.css"
    - path: "templates/components/navbar.html"
      provides: "Responsive top navigation header component"
    - path: "templates/components/footer.html"
      provides: "Page footer component"
    - path: "templates/components/alerts.html"
      provides: "Django messages alert container component"
  key_links:
    - from: "templates/base.html"
      to: "static/css/dist/styles.css"
      via: "{% static 'css/dist/styles.css' %}"
    - from: "templates/base.html"
      to: "templates/components/navbar.html"
      via: "{% include 'components/navbar.html' %}"
---

<objective>
Configure Tailwind CSS 3.4 CLI compilation via npm scripts and build the base HTML template (`templates/base.html`) with a Minimal White & Gray UI theme, responsive navbar, alert messages block, and footer.

Purpose: Provide a polished, responsive frontend layout system for server-rendered Django templates across all features.
Output: Working Tailwind CSS pipeline and clean, modular HTML layout templates.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-foundation-base-architecture/01-CONTEXT.md
@.planning/phases/01-project-foundation-base-architecture/01-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup Tailwind CSS 3.4 CLI and package.json build pipeline</name>
  <files>package.json, tailwind.config.js, static/css/src/input.css, static/css/dist/styles.css</files>
  <read_first>.planning/phases/01-project-foundation-base-architecture/01-CONTEXT.md</read_first>
  <action>
    1. Create `package.json` with devDependencies: `tailwindcss@^3.4.0`.
       Add scripts:
       - `"build:css": "tailwindcss -i ./static/css/src/input.css -o ./static/css/dist/styles.css --minify"`
       - `"watch:css": "tailwindcss -i ./static/css/src/input.css -o ./static/css/dist/styles.css --watch"`
    2. Create `tailwind.config.js`:
       ```javascript
       /** @type {import('tailwindcss').Config} */
       module.exports = {
         content: [
           './templates/**/*.html',
           './apps/**/templates/**/*.html',
           './static/js/**/*.js',
         ],
         theme: {
           extend: {
             fontFamily: {
               sans: ['Inter', 'system-ui', '-apple-system', 'BlinkMacSystemFont', 'Segoe UI', 'Roboto', 'sans-serif'],
             },
           },
         },
         plugins: [],
       }
       ```
    3. Create `static/css/src/input.css`:
       ```css
       @tailwind base;
       @tailwind components;
       @tailwind utilities;
       ```
    4. Create placeholder `static/css/dist/styles.css`.
  </action>
  <verify>test -f tailwind.config.js && test -f static/css/src/input.css</verify>
  <acceptance_criteria>
    - package.json contains "tailwindcss" and "build:css" script
    - tailwind.config.js contains content paths for ./templates/**/*.html
    - static/css/src/input.css contains @tailwind directives
  </acceptance_criteria>
  <done>Tailwind CSS configuration and static source files established</done>
</task>

<task type="auto">
  <name>Task 2: Build Minimal White & Gray base layout, navbar, alerts, and footer components</name>
  <files>templates/base.html, templates/components/navbar.html, templates/components/footer.html, templates/components/alerts.html</files>
  <read_first>.planning/phases/01-project-foundation-base-architecture/01-CONTEXT.md, templates/base.html</read_first>
  <action>
    1. Create `templates/components/navbar.html`:
       - Minimal White & Gray navbar with clean border (`border-b border-gray-200 bg-white`).
       - Logo / Product Title ("Faculty Attendance SaaS").
       - Navigation links container.
       - Mobile menu button toggle.
    2. Create `templates/components/footer.html`:
       - Minimal footer (`border-t border-gray-200 bg-white py-6 text-sm text-gray-500`).
       - Copyright and essential links.
    3. Create `templates/components/alerts.html`:
       - Renders Django `messages` using Tailwind status alerts (Success: green-50 border-green-200 text-green-800, Error: red-50 border-red-200 text-red-800, Info: blue-50 border-blue-200 text-blue-800).
    4. Create `templates/base.html`:
       - `<!DOCTYPE html><html lang="en">` with `head` loading static `css/dist/styles.css` using `{% load static %}`.
       - Google Font (Inter) inclusion or system-ui fallback.
       - Body wrapper with `bg-gray-50 text-gray-900 min-h-screen flex flex-col`.
       - `{% include "components/navbar.html" %}`.
       - Main wrapper: `<main class="flex-grow max-w-7xl w-full mx-auto px-4 sm:px-6 lg:px-8 py-8">`.
       - `{% include "components/alerts.html" %}`.
       - Content block: `{% block content %}{% endblock %}`.
       - `{% include "components/footer.html" %}`.
       - Extra JS block: `{% block extra_js %}{% endblock %}`.
  </action>
  <verify>grep -q "css/dist/styles.css" templates/base.html</verify>
  <acceptance_criteria>
    - templates/base.html includes static 'css/dist/styles.css'
    - templates/base.html includes navbar, alerts, and footer components
    - templates/components/alerts.html loops over messages
    - templates/components/navbar.html has border-b border-gray-200 minimal styling
  </acceptance_criteria>
  <done>Base HTML layout and Minimal White & Gray UI components created</done>
</task>

</tasks>

<verification>
- [ ] `templates/base.html` exists and loads `css/dist/styles.css` cleanly
- [ ] `templates/components/navbar.html`, `templates/components/footer.html`, and `templates/components/alerts.html` exist and render without syntax errors
</verification>

<success_criteria>
- All tasks completed successfully
- Tailwind CSS 3.4 setup configured with npm script entry points
- Minimal White & Gray HTML template hierarchy established
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-foundation-base-architecture/01-02-SUMMARY.md`
</output>
