---
phase: 03-multi-tenant-subdomain-infrastructure
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - config/settings/base.py
  - apps/tenants/tests_middleware.py
autonomous: true
requirements:
  - TENANT-01
must_haves:
  truths:
    - "TenantMiddleware is registered in config/settings/base.py MIDDLEWARE loading order"
    - "Django system check completes with 0 errors"
    - "Automated unit test suite verifies subdomain resolution, invalid subdomain redirection, and tenant query scoping"
  artifacts:
    - path: "config/settings/base.py"
      provides: "Django settings with registered TenantMiddleware"
    - path: "apps/tenants/tests_middleware.py"
      provides: "Unit tests for TenantMiddleware, subdomain resolution, and TenantModel query scoping"
  key_links:
    - from: "config/settings/base.py"
      to: "apps/tenants/middleware.py"
      via: "'apps.tenants.middleware.TenantMiddleware'"
---

<objective>
Register `TenantMiddleware` in Django settings, configure ALLOWED_HOSTS for subdomain routing, and implement a complete automated test suite validating multi-tenant subdomain resolution, invalid subdomain redirects, and query scoping.

Purpose: Validate that multi-tenant subdomain routing and query isolation work reliably across all execution paths.
Output: Registered `TenantMiddleware` in `config/settings/base.py` and passing test suite in `apps/tenants/tests_middleware.py`.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-multi-tenant-subdomain-infrastructure/03-CONTEXT.md
@.planning/phases/03-multi-tenant-subdomain-infrastructure/03-RESEARCH.md
@.planning/phases/03-multi-tenant-subdomain-infrastructure/03-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register TenantMiddleware in Django settings and verify Django system check</name>
  <files>config/settings/base.py</files>
  <read_first>config/settings/base.py, apps/tenants/middleware.py</read_first>
  <action>
    Update `config/settings/base.py`:
    - Add `'apps.tenants.middleware.TenantMiddleware'` to `MIDDLEWARE` array right after `MessageMiddleware` (and before `XFrameOptionsMiddleware`).
    - Verify `ALLOWED_HOSTS` includes `127.0.0.1`, `localhost`, `.localhost`, `.ourapp.com`.
  </action>
  <verify>python manage.py check --settings=config.settings.local</verify>
  <acceptance_criteria>
    - config/settings/base.py includes "apps.tenants.middleware.TenantMiddleware"
    - python manage.py check --settings=config.settings.local reports 0 issues
  </acceptance_criteria>
  <done>TenantMiddleware registered in Django settings and verified by system check</done>
</task>

<task type="auto">
  <name>Task 2: Build comprehensive automated unit test suite for TenantMiddleware and TenantModel scoping</name>
  <files>apps/tenants/tests_middleware.py</files>
  <read_first>apps/tenants/middleware.py, apps/tenants/models.py, apps/tenants/context.py</read_first>
  <action>
    Create `apps/tenants/tests_middleware.py`:
    1. Define a dummy concrete model `DummyTenantModel(TenantModel)` for testing `TenantModel` query scoping.
    2. Write `TenantMiddlewareTests(TestCase)`:
       - `test_root_domain_request`: Request to `localhost:8000` or `127.0.0.1` -> `request.tenant` is `None`.
       - `test_valid_subdomain_request`: Create `School(name="Alpha", subdomain="alpha")`. Request to `HTTP_HOST="alpha.localhost:8000"` -> `request.tenant` equals `school_alpha`.
       - `test_invalid_subdomain_redirect`: Request to `HTTP_HOST="nonexistent.localhost:8000"` -> Returns status code 302 redirecting to `http://localhost:8000/` and contains "School tenant not found" flash message.
       - `test_reserved_subdomain_ignored`: Request to `HTTP_HOST="www.localhost:8000"` or `HTTP_HOST="admin.localhost:8000"` -> `request.tenant` is `None` (not resolved as school tenant).
    3. Write `TenantModelQueryScopingTests(TestCase)`:
       - Create `school_a` and `school_b`.
       - Create `dummy_a` under `school_a` and `dummy_b` under `school_b`.
       - Test with `set_current_tenant(school_a)` -> `DummyTenantModel.objects.all()` returns only `dummy_a`.
       - Test with `set_current_tenant(school_b)` -> `DummyTenantModel.objects.all()` returns only `dummy_b`.
       - Test with `set_current_tenant(None)` -> `DummyTenantModel.objects.all()` returns all objects or empty per design.
       - Test `DummyTenantModel.objects.unscoped()` -> returns both `dummy_a` and `dummy_b`.
  </action>
  <verify>python manage.py test apps.tenants.tests_middleware --settings=config.settings.local</verify>
  <acceptance_criteria>
    - apps/tenants/tests_middleware.py contains tests for root domain, valid subdomain, invalid subdomain redirect, reserved subdomain, and query scoping
    - All tests in apps.tenants.tests_middleware pass cleanly
  </acceptance_criteria>
  <done>Automated test suite created and all multi-tenancy tests passing</done>
</task>

</tasks>

<verification>
- [ ] `python manage.py check --settings=config.settings.local` runs cleanly with 0 errors
- [ ] `python manage.py test apps.tenants.tests_middleware --settings=config.settings.local` passes 100%
</verification>

<success_criteria>
- All tasks completed successfully
- Middleware registered in loading order
- Test suite covers all multi-tenant routing and isolation paths
</success_criteria>

<output>
After completion, create `.planning/phases/03-multi-tenant-subdomain-infrastructure/03-02-SUMMARY.md`
</output>
