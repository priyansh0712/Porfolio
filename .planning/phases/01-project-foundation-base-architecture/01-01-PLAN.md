---
phase: 01-project-foundation-base-architecture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - manage.py
  - config/__init__.py
  - config/urls.py
  - config/wsgi.py
  - config/asgi.py
  - config/settings/__init__.py
  - config/settings/base.py
  - config/settings/local.py
  - config/settings/production.py
  - .env.example
  - pyproject.toml
  - apps/__init__.py
  - apps/core/__init__.py
  - apps/core/apps.py
  - apps/core/models.py
  - apps/tenants/__init__.py
  - apps/accounts/__init__.py
  - apps/public/__init__.py
  - apps/faculty/__init__.py
  - apps/biometrics/__init__.py
  - apps/schedules/__init__.py
  - apps/attendance/__init__.py
  - apps/reports/__init__.py
autonomous: true
requirements:
  - Core project foundation
must_haves:
  truths:
    - "Django application initializes and runs with python manage.py check --settings=config.settings.local"
    - "PostgreSQL database settings load from .env using django-environ"
    - "Modular apps/ directory structure is importable across the project"
  artifacts:
    - path: "manage.py"
      provides: "Django CLI entry point configured with sys.path inclusion for apps/"
    - path: "config/settings/base.py"
      provides: "Shared Django configuration powered by django-environ"
    - path: "config/settings/local.py"
      provides: "Development Django settings using local PostgreSQL"
    - path: ".env.example"
      provides: "Environment variable template for database and secrets"
    - path: "apps/core/models.py"
      provides: "Base timestamped model mixin for tenant entities"
  key_links:
    - from: "manage.py"
      to: "apps/"
      via: "sys.path.insert(0, os.path.join(BASE_DIR, 'apps'))"
    - from: "config/settings/local.py"
      to: "config/settings/base.py"
      via: "from .base import *"
---

<objective>
Initialize the Django 5.1 project with a modular apps/ directory structure, split settings (base.py, local.py, production.py) powered by django-environ, PostgreSQL database connectivity, and base model mixins.

Purpose: Provide a robust, clean backend foundation for all subsequent multi-tenant and business logic phases.
Output: Working Django project with environment configuration, PostgreSQL settings, and modular app package structure.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-foundation-base-architecture/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup Python dependencies, environment template, and split Django settings</name>
  <files>pyproject.toml, .env.example, config/__init__.py, config/urls.py, config/wsgi.py, config/asgi.py, config/settings/__init__.py, config/settings/base.py, config/settings/local.py, config/settings/production.py, manage.py</files>
  <read_first>.planning/phases/01-project-foundation-base-architecture/01-CONTEXT.md, .planning/research/STACK.md</read_first>
  <action>
    1. Create `pyproject.toml` containing dependencies: `Django>=5.1,<5.2`, `psycopg[binary]>=3.2`, `django-environ>=0.11`, `argon2-cffi>=23.1.0`.
    2. Create `.env.example` with:
       - `SECRET_KEY=django-insecure-change-this-in-production`
       - `DEBUG=True`
       - `ALLOWED_HOSTS=127.0.0.1,localhost,.localhost,.ourapp.com`
       - `DATABASE_URL=postgres://postgres:postgres@localhost:5432/studenterp1`
    3. Create `.env` copied from `.env.example`.
    4. Create `manage.py` configured with `sys.path.insert(0, os.path.join(BASE_DIR, 'apps'))` so `apps.*` packages can be imported as top-level modules or `apps.module_name`. Default `DJANGO_SETTINGS_MODULE` set to `config.settings.local`.
    5. Create `config/settings/base.py`:
       - Use `environ.Env()` to parse environment variables.
       - Set `BASE_DIR = Path(__file__).resolve().parent.parent.parent`.
       - Add `sys.path.insert(0, str(BASE_DIR / 'apps'))`.
       - Include standard Django apps (`django.contrib.admin`, `auth`, `contenttypes`, `sessions`, `messages`, `staticfiles`).
       - Add custom project apps: `apps.core`, `apps.tenants`, `apps.accounts`, `apps.public`, `apps.faculty`, `apps.biometrics`, `apps.schedules`, `apps.attendance`, `apps.reports`.
       - Set `PASSWORD_HASHERS` with `django.contrib.auth.hashers.Argon2PasswordHasher` first.
       - Set `TEMPLATES` DIR to `[BASE_DIR / 'templates']`.
       - Set `STATIC_URL = '/static/'` and `STATICFILES_DIRS = [BASE_DIR / 'static']`.
       - Set `MEDIA_URL = '/media/'` and `MEDIA_ROOT = BASE_DIR / 'media'`.
    6. Create `config/settings/local.py`:
       - Import all from `.base`.
       - `DATABASES = {'default': env.db('DATABASE_URL', default='postgres://postgres:postgres@localhost:5432/studenterp1')}`.
    7. Create `config/settings/production.py`:
       - Import all from `.base`.
       - Set `DEBUG = False`.
    8. Create `config/urls.py`, `config/wsgi.py`, `config/asgi.py`.
  </action>
  <verify>python -c "import environ; print('django-environ OK')"</verify>
  <acceptance_criteria>
    - pyproject.toml contains "Django>=5.1" and "django-environ"
    - .env.example contains DATABASE_URL=postgres://
    - config/settings/base.py contains "Argon2PasswordHasher" and TEMPLATES DIR pointing to templates/
    - config/settings/local.py contains "from .base import *"
  </acceptance_criteria>
  <done>Python dependencies and split settings architecture established</done>
</task>

<task type="auto">
  <name>Task 2: Initialize modular apps/ directory structure and core base model mixins</name>
  <files>apps/__init__.py, apps/core/__init__.py, apps/core/apps.py, apps/core/models.py, apps/tenants/__init__.py, apps/accounts/__init__.py, apps/public/__init__.py, apps/faculty/__init__.py, apps/biometrics/__init__.py, apps/schedules/__init__.py, apps/attendance/__init__.py, apps/reports/__init__.py</files>
  <read_first>config/settings/base.py, .planning/phases/01-project-foundation-base-architecture/01-CONTEXT.md</read_first>
  <action>
    1. Create `apps/__init__.py`.
    2. Create packages with `__init__.py` for:
       - `apps/core`
       - `apps/tenants`
       - `apps/accounts`
       - `apps/public`
       - `apps/faculty`
       - `apps/biometrics`
       - `apps/schedules`
       - `apps/attendance`
       - `apps/reports`
    3. Create `apps/core/apps.py`:
       ```python
       from django.apps import AppConfig

       class CoreConfig(AppConfig):
           default_auto_field = 'django.db.models.BigAutoField'
           name = 'apps.core'
       ```
    4. Create `apps/core/models.py`:
       ```python
       from django.db import models

       class TimeStampedModel(models.Model):
           """Abstract base class providing created_at and updated_at timestamps."""
           created_at = models.DateTimeField(auto_now_add=True)
           updated_at = models.DateTimeField(auto_now=True)

           class Meta:
               abstract = True
       ```
  </action>
  <verify>python manage.py check --settings=config.settings.local</verify>
  <acceptance_criteria>
    - apps/core/models.py contains "class TimeStampedModel(models.Model):"
    - apps/core/models.py contains "created_at = models.DateTimeField(auto_now_add=True)"
    - python manage.py check --settings=config.settings.local output contains "System check identified no issues"
  </acceptance_criteria>
  <done>Modular apps/ package structure initialized and validated by Django system check</done>
</task>

</tasks>

<verification>
- [ ] `python manage.py check --settings=config.settings.local` runs cleanly with 0 errors
- [ ] `apps` directory packages (`core`, `tenants`, `accounts`, `public`, `faculty`, `biometrics`, `schedules`, `attendance`, `reports`) exist and import without errors
</verification>

<success_criteria>
- All tasks completed successfully
- Split settings with django-environ verified
- Core TimeStampedModel base mixin created and importable
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-foundation-base-architecture/01-01-SUMMARY.md`
</output>
