---
phase: 01-academic-hierarchy-teacher-allocations
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - apps/academics/views.py
  - apps/academics/urls.py
  - config/urls.py
  - templates/components/navbar_admin.html
  - templates/academics/academic_hub.html
  - templates/academics/partials/tab_years.html
  - templates/academics/partials/tab_standards.html
  - templates/academics/partials/tab_subjects.html
  - templates/academics/partials/tab_allocations.html
  - templates/academics/partials/modals.html
  - apps/academics/tests.py
autonomous: true
requirements:
  - ACAD-01
  - ACAD-02
  - ACAD-03
  - ACAD-04
  - ALLOC-01
  - ALLOC-02
  - ALLOC-03

must_haves:
  truths:
    - "School Admin can view the Academic Hub at /academics/ with 4 Apple-style segmented tabs"
    - "School Admin can create, edit, delete, and set active Academic Years"
    - "School Admin can create, edit, delete Standards and Divisions with quick modal popups"
    - "School Admin can create, edit, delete Subjects with uppercase code validation"
    - "School Admin can assign and reassign Class Teachers and Subject Teachers in an interactive allocation matrix"
    - "Super Admin is forbidden (HTTP 403) from accessing /academics/"
    - "All views strictly enforce SchoolAdminRequiredMixin"
  artifacts:
    - apps/academics/views.py
    - apps/academics/urls.py
    - templates/academics/academic_hub.html
    - templates/academics/partials/tab_years.html
    - templates/academics/partials/tab_standards.html
    - templates/academics/partials/tab_subjects.html
    - templates/academics/partials/tab_allocations.html
    - templates/academics/partials/modals.html
  key_links:
    - "Academic views use SchoolAdminRequiredMixin for tenant authentication"
    - "Admin navbar includes Academics link in templates/components/navbar_admin.html"
    - "Templates adhere strictly to Apple Design System (DESIGN-apple.md / 01-UI-SPEC.md)"
---

<objective>
Implement the School Admin Academic Management Hub views, URL routing, Apple Design System UI templates, interactive allocation matrix, and end-to-end integration tests.

Purpose: Deliver an intuitive, elegant, and responsive management hub for school administrators to configure academic sessions, class divisions, subjects, and teacher allocations without leaving the dashboard.
Output: Complete `/academics/` suite with tabbed interfaces, modal workflows, navbar navigation, and full integration test coverage.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-academic-hierarchy-teacher-allocations/01-CONTEXT.md
@.planning/phases/01-academic-hierarchy-teacher-allocations/01-RESEARCH.md
@.planning/phases/01-academic-hierarchy-teacher-allocations/01-UI-SPEC.md
@DESIGN-apple.md
@templates/components/navbar_admin.html
@apps/accounts/permissions.py
@apps/academics/models.py
@apps/academics/forms.py
@apps/academics/services.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Academic Views with SchoolAdmin Authorization</name>
  <files>apps/academics/views.py, apps/academics/urls.py, config/urls.py</files>
  <read_first>apps/accounts/permissions.py, apps/academics/models.py, apps/academics/forms.py, apps/academics/services.py</read_first>
  <action>
    1. Create `apps/academics/views.py` using `SchoolAdminRequiredMixin` for all CBVs:
       - `AcademicHubView(SchoolAdminRequiredMixin, TemplateView)`:
         - Handles `?tab=years|classes|subjects|allocations` (default: `years` or `classes`).
         - Resolves active/viewed academic session `?year=<id>` (defaults to current active year).
         - Passes `academic_years`, `standards`, `divisions`, `subjects`, `allocation_matrix`, and forms to context.
       - **Academic Year Views:**
         - `AcademicYearCreateView`, `AcademicYearUpdateView`, `AcademicYearSetCurrentView` (sets `is_current=True` which triggers atomic deactivation of other years), `AcademicYearDeleteView` (with `models.PROTECT` friendly error handling).
       - **Standard Views:**
         - `StandardCreateView`, `StandardUpdateView`, `StandardDeleteView`.
       - **Division Views:**
         - `DivisionCreateView`, `DivisionUpdateView`, `DivisionDeleteView`.
       - **Subject Views:**
         - `SubjectCreateView`, `SubjectUpdateView`, `SubjectDeleteView`.
       - **Allocation Views:**
         - `ClassTeacherAssignView`: Processes assignment via `AcademicService.assign_class_teacher`.
         - `SubjectTeacherAssignView`: Processes assignment via `AcademicService.assign_subject_teacher`.
         - `SubjectTeacherDeleteView`: Removes a subject allocation.
    2. Create `apps/academics/urls.py` with namespaced endpoints (`app_name = 'academics'`).
    3. In `config/urls.py`, include `path('academics/', include('apps.academics.urls', namespace='academics'))` under tenant URLs.
  </action>
  <verify>python -c "import django; django.setup(); from apps.academics import views, urls; print('Views & URLs loaded successfully')"</verify>
  <acceptance_criteria>
    - `apps/academics/views.py` implements all CRUD and allocation CBVs with `SchoolAdminRequiredMixin`
    - `apps/academics/urls.py` defines all named routes
    - `config/urls.py` includes `apps.academics.urls`
  </acceptance_criteria>
  <done>All academic views and URL routes are configured with Layer 2 view authorization.</done>
</task>

<task type="auto">
  <name>Task 2: Build Apple Design System UI Templates & Navigation Integration</name>
  <files>templates/components/navbar_admin.html, templates/academics/academic_hub.html, templates/academics/partials/tab_years.html, templates/academics/partials/tab_standards.html, templates/academics/partials/tab_subjects.html, templates/academics/partials/tab_allocations.html, templates/academics/partials/modals.html</files>
  <read_first>.planning/phases/01-academic-hierarchy-teacher-allocations/01-UI-SPEC.md, DESIGN-apple.md, templates/components/navbar_admin.html</read_first>
  <action>
    1. In `templates/components/navbar_admin.html`:
       - Add `Academics` nav link pointing to `{% url 'academics:hub' %}` in both desktop and mobile navigation sections.
    2. Create `templates/academics/academic_hub.html`:
       - Page Canvas: `#f5f5f7`, SF Pro typography, tight tracking headlines.
       - Header with Session Switcher dropdown (`?year={{ year.id }}`).
       - Apple-style segmented pill tabs:
         - `🗓️ Academic Years` (`?tab=years`)
         - `🏫 Standards & Divisions` (`?tab=classes`)
         - `📚 Subjects` (`?tab=subjects`)
         - `👨‍🏫 Teacher Allocations` (`?tab=allocations`)
    3. Create modular partials under `templates/academics/partials/`:
       - `tab_years.html`: Clean white card list of academic years, status badges (`Active Current Session`), "+ New Academic Year" CTA, and "Set as Active" action.
       - `tab_standards.html`: Grid of Standard cards with sort badge, division pill badges, "+ Add Standard", and inline "+ Add Division" modal trigger.
       - `tab_subjects.html`: Table of subjects with monospace code badges, type tags (Core/Elective), and "+ Add Subject" button.
       - `tab_allocations.html`: Class-wise allocation matrix cards with Class Teacher banner (avatar + badge + change modal) and Subject Teacher rows with assign buttons.
       - `modals.html`: Reusable Apple-style modal dialogs with smooth transitions, ESC key dismiss, backdrop blur, and Django CSRF form targets for all Create/Edit actions.
  </action>
  <verify>python -c "import django; django.setup(); from django.template.loader import render_to_string; print('Templates rendered successfully')"</verify>
  <acceptance_criteria>
    - `templates/components/navbar_admin.html` contains the `Academics` navigation link
    - `templates/academics/academic_hub.html` and partials exist and adhere to `01-UI-SPEC.md`
    - Segmented tab control seamlessly switches views without JavaScript errors
  </acceptance_criteria>
  <done>Apple Design System UI templates render beautifully with responsive modals and segmented controls.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Integration Tests & Verify Full Test Suite</name>
  <files>apps/academics/tests.py</files>
  <read_first>apps/academics/views.py, apps/academics/models.py, apps/accounts/permissions.py</read_first>
  <action>
    Extend `apps/academics/tests.py` with full integration & view security tests:
    1. `AcademicViewSecurityTests`:
       - Unauthenticated requests to `/academics/` redirect to `/login/`.
       - Super Admin request to `/academics/` returns HTTP 403 Forbidden.
       - Faculty user request to `/academics/` returns HTTP 403 Forbidden.
       - School Admin user of Tenant A accessing Tenant A `/academics/` returns HTTP 200 OK.
       - School Admin user of Tenant B attempting to access Tenant A `/academics/` returns HTTP 403 Forbidden.
    2. `AcademicCRUDViewTests`:
       - Create, Edit, Delete AcademicYear via POST requests.
       - Set Current AcademicYear toggles `is_current` properly.
       - Create Standard and Division via POST requests with unique constraints.
       - Create Subject with uppercase code validation.
       - Class Teacher assignment updates allocation record.
       - Subject Teacher assignment updates allocation record.
       - Deleting standard/division with existing allocations handles `ProtectedError` gracefully.
  </action>
  <verify>python manage.py test apps.academics && python manage.py test</verify>
  <acceptance_criteria>
    - `apps/academics/tests.py` covers all view authorization, CRUD, and allocation flows
    - `python manage.py test apps.academics` passes with 0 errors
    - Full project test suite passes 100%
  </acceptance_criteria>
  <done>All integration tests and full test suite pass green with zero regressions.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python manage.py check` reports no system issues
- [ ] `python manage.py test apps.academics` passes 100%
- [ ] `python manage.py test` passes full test suite (165+ tests)
- [ ] Manual check of `/academics/` shows pixel-perfect Apple Design System layout
</verification>

<success_criteria>
- Academic Hub live at `/academics/` with all 4 tabs functional
- Modals for creating/editing Standards, Divisions, Subjects, and Allocations work seamlessly
- Full multi-tenant isolation and 3-layer security verified
- Full test suite passes green
</success_criteria>

<output>
After completion, create `.planning/phases/01-academic-hierarchy-teacher-allocations/01-02-SUMMARY.md`
</output>
