# Plan 04-02: Self-Service Password Security & End-to-End Verification

## Phase: Phase 4 - Faculty & Student Views, Password Security & Integration Polish
## Plan ID: 04-02
## Objective: Implement self-service password changing (`/accounts/password/change/`) for Faculty and Students, enforce multi-tenant permission decorators, and execute full end-to-end security test verification.

---

### Key Deliverables:
1. `apps/accounts/views.py`:
   - `SelfPasswordChangeView`: Password update view utilizing Django's `PasswordChangeForm` with session auth hash updates.
2. `apps/accounts/urls.py`:
   - Registered route `/accounts/password/change/`.
3. `templates/accounts/password_change.html`:
   - Apple Design System password change template with success and error alert banners.
4. `apps/faculty/tests.py` & `apps/accounts/tests.py`:
   - Unit tests for "My Class", "My Subjects", password updating, and tenant isolation security.

---

### Verification:
- Run `python manage.py test` (Ensure 100% test pass rate across all project unit tests).
- Log in as Faculty/Student, change password, and verify seamless re-authentication.
